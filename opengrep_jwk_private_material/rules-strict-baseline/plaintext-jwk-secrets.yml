rules:
  - id: plaintext-jwk-private-material
    message: "Hardcoded private JWK material in plaintext (strict)"
    severity: WARNING
    languages: [generic]
    paths:
      include:
        - "**/*.txt"
        - "**/*.conf"
        - "**/*.ini"
        - "**/*.env"
        - "**/*.yaml"
        - "**/*.yml"
        - "**/*.toml"
        - "**/*.cfg"
        - "**/*.config"
        - "**/*.properties"
        - "**/*.tfvars"
        - "**/*.tfvars.json"
    metadata:
      category: security
      cwe:
        - CWE-798
      confidence: MEDIUM
      likelihood: MEDIUM
      impact: HIGH
      description: >-
        Detects JSON-shaped JWK blobs in config or text files that include
        private key fields (strict baseline profile). Canonical rule pack:
        https://github.com/Endor-Solutions-Architecture/scripts/tree/main/opengrep_jwk_private_material
      display-name: Plaintext / config hardcoded private JWK (strict baseline)
      endor-category: vulnerability
      rule-origin-note: github-esa-scripts-opengrep-jwk-private-material
      endor-tags:
        - jwk
        - cwe-798
        - hardcoded-credentials
        - secret-detection
        - jwk-profile-strict-baseline
      references:
        - https://datatracker.ietf.org/doc/html/rfc7517
        - https://datatracker.ietf.org/doc/html/rfc7518
    pattern-either:
      - pattern-regex: '"kty":"RSA"[\\s\\S]{0,1600}"d":"[A-Za-z0-9_-]{32,}"'
      - pattern-regex: '"kty":"EC"[\\s\\S]{0,1600}"d":"[A-Za-z0-9_-]{32,}"'
      - pattern-regex: '"kty":"OKP"[\\s\\S]{0,1600}"d":"[A-Za-z0-9_-]{32,}"'
      - pattern-regex: '"kty":"RSA"[\\s\\S]{0,1600}"p":"[A-Za-z0-9_-]{32,}"[\\s\\S]{0,1600}"q":"[A-Za-z0-9_-]{32,}"[\\s\\S]{0,1600}"dp":"[A-Za-z0-9_-]{32,}"[\\s\\S]{0,1600}"dq":"[A-Za-z0-9_-]{32,}"'
      - pattern-regex: '"kty":"oct"[\\s\\S]{0,1600}"k":"[A-Za-z0-9_-]{32,}"'
